from main.Action import Action
from main.Agent import Agent
from main.Maze import Maze
from main.QLearning import QLearning

class MyEGreedy:

    def getRandomAction(self, agent, maze):
        # TODO to select an action at random in State s
        return None

    def getBestAction(self, agent, maze, q_learning):
        # TODO to select the best possible action currently known in State s.
        return None

    def getEGreedyAction(self, agent, maze, q_learning, epsilon):
        # TODO to select between random or best action selection based on epsilon.
        return None
